[workspace]
resolver = "2"
# prover/ (the CLI) joins once Phase 3 wires it up.
members = ["prover-core", "methods"]

# Always optimize; building and running the guest takes much longer without optimization.
[profile.dev]
opt-level = 3

[profile.release]
debug = 1
lto = true
